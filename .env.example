# Gemini API Key for AI email generation features
# For local development: Copy this file to .env.local and add your actual key
# For production: Pass as build arg to Docker build or via Cloud Build substitutions
# WARNING: Client-side API keys are visible in browser. For production, use server-side proxy.
VITE_GEMINI_API_KEY=

# Firebase configuration (if needed)
# VITE_FIREBASE_API_KEY=
# VITE_FIREBASE_AUTH_DOMAIN=
# VITE_FIREBASE_PROJECT_ID=
